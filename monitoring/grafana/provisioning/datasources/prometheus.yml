apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Fixed uid so the committed dashboard JSON can reference it.
    uid: prometheus
    access: proxy
    # Service name on the compose network, not localhost.
    url: http://prometheus:9090
    isDefault: true
