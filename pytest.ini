[pytest]
# Imports in this project are absolute from the repository root
# (`from src.testing... `, `from Ammeters...`), so the root has to be importable
# regardless of how pytest was invoked - `pytest` on PATH does not add the working
# directory the way `python -m pytest` does.
pythonpath = .
testpaths = tests
