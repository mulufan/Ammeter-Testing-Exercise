# Runtime dependencies.
#   pyyaml     - config/config.yaml, loaded by src/utils/config.py (supplied).
#   matplotlib - the measurement-series plot in src/testing/visualization.py;
#                the only dependency this project added, see
#                IMPLEMENTATION_NOTES.md, "3. Result Analysis - bonus".
pyyaml>=6.0
matplotlib>=3.4.0

# Not imported anywhere. Left from the supplied file and still to be removed
# under ISS-21.
numpy>=1.21.0
scipy>=1.7.0
seaborn>=0.11.0
pandas>=1.3.0

# Test dependencies. These are for the suite under tests/ and the CI coverage gate.
pytest>=7.0
pytest-cov>=4.0
